.onLoad = function (libname, pkgname) {
    .createCache()
    .initializeProgressBarOnLoad()
}

# .onAttach <- function(libname, pkgname) {
#     msg <- 'A ginmappeR web app is also available at https://gin.us.es'
#     packageStartupMessage(msg)
# }
